 <div class="img-upload">
      <div class="weight-bold-text">
        お酒の画像
        <span class="indispensable">任意</span>
      </div>
      <div class="click-upload">
        <p>
          クリックしてファイルをアップロード
        </p>
         <%= file_field :image, id:"item.image" %>
      </div>
    </div>

   
   <%= form_with(model: item, local: true) do |form| %>
      <%= form.text_field :name, placeholder: "酒蔵名（必須、最大40文字まで）" %>
      <%= form.text_field :name, placeholder: "お酒の名前（必須、最大40文字まで）" %>
      <%= form.text_field :name, placeholder: "飲み口（必須、甘口や辛口など）"%>
      <%= form.text_field :name, placeholder: "オススメの飲み方（必須、冷酒や熱燗など）"%>
      <%= form.text_area :text, placeholder: "お酒の説明（必須、最大1,000文字まで）"%>
      <%= form.submit "投稿する" %>
   <% end %>